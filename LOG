From lcc 3.5 to 3.6:

Changes:

-A -A now complains if main is declared other than `int main(void)' or
`int main(int, char **)'.

Errors fixed:

Given `typedef const struct foo T; struct foo { ... }' lcc failed to
compute the size of T correctly.

$Id$
